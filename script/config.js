const conf = {
    tubeInterval: 2000, // Intervallo tra l'aggiunta di nuovi tubi (in millisecondi)
    tubeGap: 200, // Spazio tra i tubi
    gameSpeed: 2, // Velocità di avanzamento del gioco
    flapVelocity: -8 // Velocità di salto dell'uccello
};

export default conf;
